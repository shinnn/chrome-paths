branches:
  except: /^v\d/
language: node_js
node_js: node
matrix:
  include:
  - addons:
      chrome: stable
  - os: osx
    addons:
      chrome: stable
    script: node test.js
  - os: windows
    before_install: choco install googlechrome
    script: node test.js
